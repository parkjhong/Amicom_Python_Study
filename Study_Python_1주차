from random import *

#1
number = (int)(random() * 29) + 1
print("%d 개" %number)

#2
print("  A\n BC\nDEF")

#3
age = randint(10, 30)
name = "kelvin"
print("안녕하세요 제 이름은 %c%s 입니다. 제 나이는 %d입니다." %(name[0].upper(), name[1:], age))
